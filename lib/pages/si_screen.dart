import 'package:flutter/material.dart';

class SiScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Center(child: new Text("死"),), ),
      body: Center(child: new Text("死"),),
    );
  }
}