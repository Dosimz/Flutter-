import 'package:flutter/material.dart';

class SanScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Center(child: new Text("三"),), ),
      body: Center(child: new Text("三"),),
    );
  }
}